{
	// See https://go.microsoft.com/fwlink/?LinkId=733558
	// for the documentation about the tasks.json format
	"version": "2.0.0",
	"tasks": [
		{
			// NOT WORKING! DON't know why... It cannot generate pdf although eps is updated, nor can it open pdf in GUI
			// a workaround is to open eps file using `refreshable-pdfopen xxx.eps` and then use this task with shortcut "Ctrl+G"
			//    but just remember, since it's not updating pdf file, when using in paper, be careful to update them!

			"label": "Build and View Gnuplot",
			// "type": "shell",
			// "command": "echo ${fileDirname}",
			"command": "bash",
			// "command": "gnuplot ${fileBasenameNoExtension}.gp && refreshable-pdfopen ${fileBasenameNoExtension}.pdf",
			// "args": ["-c", "gnuplot ${fileBasenameNoExtension}.gp && refreshable-pdfopen ${fileBasenameNoExtension}.pdf"],
			"args": ["-c", "gnuplot ${fileBasenameNoExtension}.gp"],
			"presentation": {
				"reveal": "always",
				"panel": "dedicated",
				"clear": true
			},
			"options": {
				"cwd": "${fileDirname}"
			}
		}
	]
}
